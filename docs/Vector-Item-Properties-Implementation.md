# Vector Item Properties Implementation Notes

Last Updated: 09-12-26 09:58 · Revision 1.5

Implements the shared vector item properties that General UI Phase 6 decision 6.2 named and deferred (Stroke Style, Fill Opacity, Stroke Opacity, the Shadow section, Blend Mode, and Line Spacing), from the Basic Shape Annotation Tools PRD (`PRDs/SnapMock-Basic-Shape-Annotation-Tools-PRD.html`, version 1.5 at the start), the Text and Callout Annotation Tools PRD (version 1.6), the Blur, Highlighter, and Eyedropper Tools PRD (version 1.3), the General UI PRD (version 2.12), and the Technical Architecture PRD (version 1.24), in the three phases and the close-out defined by `docs/Vector-Item-Properties-Kickoff-Prompt.md` (revision 1.0). A session pasting that prompt starts at the first phase not marked done in Section 1. `docs/General-UI-Implementation-Kickoff-Prompt.md` (revision 1.1) governs the standards; the General UI implementation notes (`docs/General-UI-Implementation.md`) hold the walk table of Section 17.2, which this work does not expect to extend.

Starting state, verified at commit 57bdbd6 on 09-11-26 (the kickoff names c1c35c5; 57bdbd6 adds only the kickoff prompt and the marker notes 1.4 pointer): `VectorItem` carries `stroke_color`, `stroke_width`, `fill_color`, and the base class's one `opacity`; `ShadowMixin` is mixed into the three marker items only; `NumberedStepItem` alone carries `fill_opacity` and `stroke_opacity`; `TextItem` and `CalloutItem` sit beside `VectorItem` with their own background and border properties; the Highlighter paints its own round-capped pen with no composition mode; the Arrow draws one filled triangle; the Rectangle's `corner_radius` has no control. The suite at the last code commit (12aad72) ran 1191 tests with 13 skipped and one environmental deselection; ruff and mypy are clean.

## 1. Phase status

| Phase | Scope | Status | Commits |
|---|---|---|---|
| 1 | The shared properties (Basic Shape PRD 2.2, 2.6; Technical Architecture PRD 4.3): the decisions, `VectorItem` and the items, the text items, the Tool Options Bar and presets, the Property Panel, close-out | Done | 7dc2106, 1f3fe67, e969344, 3e850d7, 1ec594a, then this close-out commit |
| 2 | Blend mode and line spacing (Blur PRD 3.4 to 3.7; Text PRD 2.2; General UI PRD 8.3, 8.4): the Highlighter, the item blend mode, line spacing, close-out | Done | 099d4d0 (steps 1 to 3), then this close-out commit |
| 3 | The per-tool controls (Basic Shape PRD 4.3, 4.4, 4.7, 5.3, 5.4): arrowheads, corner radius, close-out | Done | ab71e15 (steps 1 and 2), then this close-out commit |

## 2. Decisions

### 2.1 Taken at the start of Phase 1 (09-11-26)

Presented with the consequential decision template and chosen by Doug on 09-11-26: the recommendation in every case.

| Decision | Choice | Effect |
|---|---|---|
| 1 Where the shared properties live for the text items | B, mixed in beside `VectorItem` | `TextItem` and `CalloutItem` stay `RichTextMixin, SnapGraphicsItem` and mix in `ShadowMixin` and the two opacities directly, as the stamp and emoji items do. The shadow is drawn first, under the text box's frame and under the callout's combined bubble and tail path (the bubble plus its tail circles for the cloud shape), with the Basic Shape PRD's defaults (`#00000080`, offset 3 and 3, blur 5). The background is the fill for `fill_opacity` and the border is the stroke for `stroke_opacity`; their `border_style` keeps its name. The Property Panel's Appearance section stays hidden for text items; the Text Box section gains Fill Opacity and Stroke Opacity rows and keeps Background and Border; the Shadow section shows for them. The Text tool's and the Callout tool's Tool Options Bars gain Border Style (Text PRD 3.5, 4.6). Technical Architecture PRD 4.2 gains a row saying the two text items sit beside `VectorItem` rather than under it; Text PRD 3.6's "Transform, Appearance, Shadow, and Text sections" is departed from on Appearance. The cost: "vector item" means the class and the property set, and a text box's opacities live in a different section from a rectangle's. The alternatives: option A, the class move (background to fill, border to stroke, every text and callout test, the Snagit field names, old files' keys read as fallbacks) for what is a naming change; option C, defer, which would leave the Text PRD's 3.6 and 8.3 rows unmet. |
| 2 What the vector items' Opacity control becomes | A, replace | Vector items and their tools expose `fill_opacity` and `stroke_opacity` and no `opacity_pct`; the Appearance section shows the two sliders for a vector selection and the one slider for a non-vector selection; a stored preset, theme, or session state carrying `opacity_pct` for a shape tool is read once into both new keys, when neither is present, and dropped on the next write; the base `opacity` stays as a Qt property for groups, non-vector items, and the layer machinery. A selection that holds a group shows the single Opacity slider for the group's own opacity beside the two sliders acting on its vector members, so nothing loses a control. The Snagit writer keeps writing the base opacity (kickoff silence 10), which is 100 for a shape from now on. The cost: the migration rule and a saved preset whose meaning changes from one number to two. The alternative, option B, three opacity controls on every shape and the numbered step's deviation made permanent. |
| 3 How far into the Basic Shape tools this kickoff goes | B, Phases 1 to 3 as written | Arrowheads on straight lines (head and tail styles, the four sizes and the custom size; Basic Shape PRD 4.3, 4.4, 4.7) and the uniform Corner Radius control (5.4) are Phase 3. `line_style` stays Straight; curved and elbow arrows (4.5, 4.6), `corner_radius_mode` and the individual radii (5.3), point editing, the Arc and Polygon tools, and the Freehand tool's Bezier fitting and Close Path are the following kickoff's, named at the close-out. `head_style` defaults to Open per 4.3, which changes the look of every new arrow from today's filled triangle; a file saved before Phase 3 loads its arrows with the filled style, so nothing on disk changes appearance. The cost: a third phase of geometry work, and 4.5, 4.6, and 5.3 recorded as partly built. The alternatives: option A, the shared properties only, leaving the Arrow with one fixed head; option C, the whole PRD, two new tools and point editing on every shape in one notes document. |
| 4 Whether the item blend mode is built for every item or the Highlighter alone | A, every item | `blend_mode` on `SnapGraphicsItem` for every item, serialized as a string and read as Normal when absent; the Appearance section's Blend Mode dropdown lists the layer's seven names plus Soft Light, which the Highlighter needs (Blur PRD 3.4) and the layer list lacks; `composition_mode` in `core/layer.py` gains Soft Light. The combination rule, recorded in Technical Architecture PRD 3.9: the item's mode is applied as the painter composition mode when the layer's is Normal, the layer's otherwise, one composition mode per paint. The Highlighter's default is Multiply and the shadow is painted before the mode is set (Blur PRD 3.7). The SVG export drops the mode for every item, as it drops the layer's. The cost: the rule, a key on every item, and the export row. The alternative, option B, the Highlighter alone, with 3.1.4 and 8.3 departed from for every other item. |

### 2.2 The kickoff's twelve silences

Each decided as the kickoff recommended, on 09-11-26.

| Silence | Decision |
|---|---|
| `stroke_style` and `border_style` | One enum, `BorderStyle` in `config/constants.py`; `VectorItem.stroke_style` holds it under the key `stroke_style`; the text items keep the `border_style` name and key. |
| The shadow defaults | Each PRD's own: the Basic Shape PRD's (`#00000080`, offset 3 and 3, blur 5) for the shapes and the text items; the marker PRD's for the three marker items. `_init_shadow` takes the defaults as arguments and `constants.py` gains the vector set beside the marker set. |
| The stroke cap and join | `stroke_cap` Round and `stroke_join` Round by default for every vector item except the Highlighter (Flat cap); `pen()` applies them; the arrowhead's Open style keeps a round cap. No bar control for cap or join except the Highlighter's Cap Style toggles (Phase 2) and the Freehand's Stroke Cap toggles (9.6); no Property Panel row for either (not in General UI PRD 8.3). |
| The two opacities and `hit_shape` | A fill with `fill_opacity` 0 is still a fill for hit testing; `hit_shape` keys on `fill_color.alpha()` as today. |
| The preset migration of decision 2 | `decode_values` maps a shape tool's `opacity_pct` to `fill_opacity` and `stroke_opacity` (the same fraction) when neither new key is present, once, on read; `PREFERENCE_KEYS` unchanged. |
| The Highlighter's opacity | `highlight_color`'s alpha is primary (Blur PRD 3.7); the Highlighter tool's bar drops `opacity_pct` with the others and its `stroke_color` is the highlight colour; the tool's controls become Highlight Color, Stroke Width, Blend Mode, Cap Style, Stroke Style, and the preset colour row (Phase 2). |
| Auto-Straighten and Snap to Axis | Not shown: `HighlightTool` does not straighten strokes today (verified at c1c35c5), and a control with nothing behind it cannot be greyed out (General UI PRD 1.3). A Blur PRD row records that the behaviour belongs to a Highlighter kickoff. |
| Line Spacing's command | While editing, the paragraph at the cursor through the editor's `QTextCursor` as a `TextFormatCommand` merge boundary; otherwise every paragraph of the selected items through `set_line_height` as one `ModifyPropertyCommand`-shaped command per item (`line_spacing` as a whole-document property: the value shown is the first paragraph's, mixed when paragraphs differ). |
| The SVG export | Shadows land as raster images inside the SVG and composition modes are dropped, as the marker work recorded; one row per PRD, no new mechanism. |
| The Snagit writer | `stroke_style`, the opacities, and the shadow are not mapped; a Snagit notes row. |
| The item blend mode's combination rule | The item's mode when the layer's is Normal, the layer's otherwise; Technical Architecture PRD 3.9. |
| Groups | A group has no properties of its own; Appearance and Shadow for a selected group act on its vector members (`_selected_vectors`) and on its shadow-carrying members (`_selected_shadowed` expanding groups), with the 8.6 mixed indicators. |

### 2.3 Findings from the read list, decided with the decisions

- The Highlighter tool's creation default for colour is the shared red stroke default (`#FF0000`) at full opacity, so a new highlight today is an opaque red stroke; the Blur PRD's 3.4 default is `#FFFF00CC` (yellow at 80 percent) and the item's own constructor default is yellow at 50 percent. Phase 2 step 1 sets the tool's default to the PRD's value and records the fix as a Blur PRD row.
- Soft Light is a Highlighter blend mode (Blur PRD 3.4) and not one of the layer's seven names (General UI PRD 7.4); decision 4 adds it to the item's list and to `composition_mode`.
- A group is neither a vector item nor a non-vector item under decision 2's rule; the follow-on detail in the decision table keeps the group's own Opacity slider beside the two.

## 3. What Phase 1 built

In commit order. Step 1, 7dc2106: the decisions of Section 2; Basic Shape PRD 1.6; Text and Callout PRD 1.7; General UI PRD 2.13; Technical Architecture PRD 1.25. Step 2, 1f3fe67: `VectorItem` mixes in `ShadowMixin` with the Basic Shape PRD's defaults (`DEFAULT_VECTOR_SHADOW_*` in `config/constants.py`) and gains `stroke_style` (the `BorderStyle` enum), `stroke_cap` and `stroke_join` (the new `StrokeCap` and `StrokeJoin` enums), `fill_opacity` and `stroke_opacity`; `pen()` carries the colour at the stroke opacity with the style, cap, and join, `brush()` the fill at the fill opacity, `stroke_outline` the stroked area with the dash pattern, `shadow_path` the stroke's area plus a filled outline's interior, `stroke_margin` the reach of a miter join or square cap, `apply_creation_defaults` the tool's keys (step 4); `_base_data` and `_apply_base_data` carry the five keys and the shadow keys, absent keys reading as the item's defaults; `RectangleItem`, `EllipseItem`, `LineItem`, `ArrowItem` (the head at the stroke opacity), `FreehandItem`, and `HighlightItem` (Flat cap; the shared pen) paint the shadow first and include it in `boundingRect`; `NumberedStepItem` drops its own `border_style`, opacities, and `_with_alpha` for the base's, `border_style` aliasing `stroke_style` under its Section 5 key. Step 3, e969344: `TextItem` and `CalloutItem` mix in `ShadowMixin` with the two opacities (decision 1), the shadow under `TextItem.frame_path` plus the border's area and under `CalloutItem.shadow_path` (the combined path, the cloud's tail circles, the border's area), the border pen through the shared `STROKE_STYLE_MAP`; the keys written and read. Step 4, 3e850d7: `SHARED_CONTROLS` gains `stroke_style`, `fill_opacity`, and `stroke_opacity` (the last two through `ControlSpec.scale`, 100 widget units per stored unit); the shape tools' `options_controls` and creation defaults per Basic Shape PRD 2.6 with `opacity_pct` dropped; the Text and Callout tools' Border Style; `_ENUM_TYPES` with `StrokeCap` and `StrokeJoin`; `migrate_opacity` and `ToolThemeManager._migrated` and `_preset_values` in `core/tool_themes.py`; the Manage Presets summary's percent. Step 5, 1ec594a: the Property Panel's Appearance section with Stroke Style, Fill Opacity, and Stroke Opacity (moved from the Numbered Step section), the single Opacity row for a group's own opacity (`_selected_non_vectors`), the Shadow section for every carrier and for groups (`_shows_shadow`, `_selected_shadowed` expanding groups), the Text Box section's two opacity rows, `_set_row_visible` for the fill rows in tool-defaults mode; the text tools carry the two opacities as creation defaults. Step 6, this commit: Basic Shape PRD 1.7, Text and Callout PRD 1.8, General UI PRD 2.14, Technical Architecture PRD 1.26 (with its 4.3 note rewritten), these notes.

Silences found while building, decided as the code says and recorded as PRD rows: the shadow of a shape whose fill is transparent follows the stroke only, so an unfilled rectangle's shadow is an outline (Basic Shape PRD 1.7); a dashed stroke's shadow is dashed; the text box's shadow duplicates the box shape whether or not the background or border is painted (Text PRD 1.8; 3.7 constructs the shape first); the Line, Arrow, Freehand, and Highlighter bars omit Fill Color and Fill Opacity, since those items draw no fill, and the Highlighter's bar has no opacity control (silence 6); the Shadow section stays hidden in the Property Panel's tool-defaults mode, where the bar's Shadow toggle is the default's control, since only `shadow_enabled` is a creation default (General UI PRD 2.14); a preset saved before a tool gained keys applies whole over the active theme's values and still reads as applied, where it read as Custom before (General UI PRD 2.14); the two opacities of the text tools are creation defaults that presets capture though their bars do not show them (Text PRD 3.5 lists none).

Two latent bugs fixed on the way, each with a PRD row: `ShadowMixin`'s stand-in `update` and `prepareGeometryChange` methods sat ahead of Qt's in the method resolution order of the three marker items and swallowed every call from a property setter (Technical Architecture PRD 1.26); `CalloutItem.boundingRect` united the bubble with a null rectangle at the tail tip, which a union drops, so the tail was outside the bounding rect (Text PRD 1.8).

## 4. What Phase 2 built

Steps 1 to 3 in one commit, 099d4d0 (a departure from the kickoff's one commit per step: the three steps share `config/constants.py`, `items/text_item.py`, `items/callout_item.py`, and `ui/property_panel.py`, and were built while the Phase 1 suite ran, so their file sets could not be committed apart). Step 1, the Highlighter: `ITEM_BLEND_MODES` and `normalize_item_blend_mode` in `core/layer.py` with Soft Light in the composition map; `blend_mode` on `SnapGraphicsItem` with the combination rule in `_apply_flip` (the item's mode when the layer's is Normal, the layer's otherwise) and `_blend_entry` and `_apply_blend_entry` for the serializers; `paint_shadow` in `items/shadow.py` drawing with normal composition under an item mode (Blur PRD 3.7); `VectorItem._base_data` and `apply_creation_defaults` carrying `blend_mode`; `HighlightItem` with the PRD's defaults (`DEFAULT_HIGHLIGHT_*` and `HIGHLIGHT_PRESET_COLORS` in `config/constants.py`), Multiply, and a `highlight_color` alias; `HighlightTool` rebuilt (creation defaults `highlight_color`, `highlight_width`, `blend_mode`, `stroke_style`, `stroke_cap`, `shadow_enabled`; `options_controls` per Blur PRD 3.5 with the tool-built Cap Style toggles with `cap_icon` previews and the six preset swatches; `_item_defaults` mapping the tool's keys to the item's); `SHARED_CONTROLS` gains `highlight_color`, `highlight_width` (10 to 80), and `blend_mode` (the four modes of 3.4). Step 2, the item blend mode: `blend_mode` written and read by every other item (`TextItem`, `CalloutItem`, `RasterRegionItem`, `BlurItem`, `StampItem`, `EmojiItem`, `GroupItem`); the Property Panel's Item Info section gains the Blend Mode dropdown over `ITEM_BLEND_MODES`, pushed to every selected item with the mixed indicator. Step 3, Line Spacing: `paragraph_line_spacings`, `_get_line_spacing`, `_set_line_spacing`, and `_set_line_spacings` on `RichTextMixin`; `line_spacing` and `line_spacings` on `TextItem` and `CalloutItem`; `DEFAULT_LINE_SPACING`, `LINE_SPACING_MIN`, and `LINE_SPACING_MAX`; the Text and Callout tools' `line_spacing` creation default applied to new items; the Text section's Line Spacing spin box, populated per paragraph for a selection and from the cursor's paragraphs while editing (`_block_line_spacing`), writing the editor's block format while editing and one `ModifyPropertyCommand` of `line_spacings` per item (a `MacroCommand` for several) otherwise. Step 4, this commit: Blur PRD 1.4, Text and Callout PRD 1.9, General UI PRD 2.15, Technical Architecture PRD 1.27, Snagit notes 1.2, these notes.

Silences found while building, decided as the code says and recorded as PRD rows: the Blend Mode dropdown sits in the Item Info section rather than Appearance, since every item carries the property and only vector items show Appearance (General UI PRD 2.15); the Highlighter's Cap Style toggles follow Stroke Style rather than preceding it, since a tool's own controls sit in the bar's one slot (Blur PRD 1.4); the bar's Blend Mode dropdown lists the Highlighter's four modes while the panel's lists all eight; the item mode's stored spelling is the layer list's (`Soft Light`) and the Blur PRD's lower-case forms are read; the Line Spacing command carries every paragraph's value so an undo restores paragraphs that differed; an item whose paragraphs have no line height reads 1.0, Qt's single spacing, so files saved before this work render as before, while new text items take the PRD's 1.2 (Text PRD 1.9); the Highlighter's default colour was the shared red stroke colour at full opacity and is now the PRD's yellow at 80 percent (Section 2.3's first finding).

## 5. What Phase 3 built

Steps 1 and 2 in one commit, ab71e15 (the two steps share `config/constants.py`, `ui/tool_options_bar.py`, `ui/property_panel.py`, and two test modules, and were built while the Phase 2 suite ran). Step 1, arrowheads (Basic Shape PRD 4.3, 4.4, 4.7, 10.2): `HeadStyle`, `HeadSize` with `HEAD_SIZE_PX`, `HEAD_SIZE_CUSTOM_MIN` and `_MAX`, and `LineStyle` in `config/constants.py`; `ArrowItem` rebuilt with `head_style` (Open), `tail_style` (None), `head_size` (Medium), `head_size_custom` (0, the named size), `line_style` (Straight), `effective_head_size` (the named or custom size plus the stroke width), `_head_geometry` and `head_paths` (the filled heads, the open heads' lines, and the shaft that remains between them: the shaft ends at a filled head's base and at the centre-back of a diamond, circle, or square), the paint drawing the shadow over the heads, the shaft, the open lines with a round cap and a solid line, and the filled heads at the stroke opacity, `apply_creation_defaults` taking the four keys, and `deserialize` reading an entry without `head_style` as Filled (decision 3); `ArrowTool`'s `options_controls` and creation defaults gain the four keys; `SHARED_CONTROLS` gains `head_style` and `tail_style` (enum dropdowns whose glyphs `arrow_head_icon` draws from the item itself), `head_size`, and `head_size_custom` (0 to 60); `_ENUM_TYPES` knows `HeadStyle` and `HeadSize`; the Snagit reader gives an imported arrow the Filled head; the Property Panel's Arrow section (Head, Tail, Head size, Custom size) for a selection of arrows. Step 2, the corner radius (5.3, 5.4): `CORNER_RADIUS_MAX`; `RectangleItem.corner_radius` stored as set from 0 to 200 and drawn through `effective_corner_radius`, clamped to half the smaller side, with `apply_creation_defaults` taking the key; the Rectangle tool's `corner_radius` control and default; the shared `corner_radius` slider (0 to 200); the Property Panel's Rectangle section with the Corner radius row. Step 3, this commit: Basic Shape PRD 1.8, General UI PRD 2.16, Technical Architecture PRD 1.28, these notes, and the close-out of the work.

Silences found while building, decided as the code says and recorded as PRD rows: the head size scales with the stroke width by addition, the rule the one fixed head used (Basic Shape PRD 1.8); an open head's lines are solid whatever the stroke style, so a dashed arrow keeps a whole head; the Line Style toggles of 4.7 are not shown, since only Straight is drawn; a placed arrow's heads and a placed rectangle's radius are edited in new Property Panel sections, since the bar edits creation defaults only (General UI PRD 2.16); the radius is stored as set and clamped when drawn, so a resize restores it; a Snagit arrow imports with the Filled head while the writer still writes EquilateralArrow (silence 10).

## 6. Deviations from the PRDs

Each has its PRD row.

- `TextItem` and `CalloutItem` sit beside `VectorItem`, not under it (decision 1; Technical Architecture PRD 4.2), and the Property Panel's Appearance section does not show for them (Text PRD 3.6): their fill and stroke opacities are rows of the Text Box section, which General UI PRD 8.4 does not list.
- The Blend Mode dropdown lists eight names (the layer's seven plus Soft Light), not the Technical Architecture PRD 3.1.4 example list (decision 4), and sits in the Property Panel's Item Info section rather than the Appearance section of General UI PRD 8.3, since every item carries the property.
- The Highlighter's Cap Style toggles follow Stroke Style in the bar rather than preceding it (Blur PRD 3.5), and Auto-Straighten and Snap to Axis are not shown, since the tool does not straighten strokes (3.3 is not built).
- New text items take the Text PRD's 1.2 line spacing; an existing item whose paragraphs have no line height reads 1.0, Qt's single spacing, not 1.2.
- The shadow offset is two floats, `shadow_offset_x` and `shadow_offset_y` (Basic Shape PRD 2.2), not the `QPointF` of Technical Architecture PRD 4.3, as the marker work already built it.
- No bar or Property Panel control for `stroke_cap` or `stroke_join` (silence 3): the Freehand tool's Stroke Cap toggles of Basic Shape PRD 9.6 wait for the following kickoff; the Highlighter's Cap Style toggles arrived in Phase 2.
- The Line, Arrow, Freehand, and Highlighter bars have no Fill Color or Fill Opacity, which Basic Shape PRD 2.6 lists for every shape tool; the items draw no fill.
- The Property Panel's tool-defaults mode hides the Shadow section (General UI PRD 8.3 lists the section for a vector tool's defaults through 8.5).
- The SVG export lands every shadow as a raster image inside the file and drops every item's blend mode as it drops the layer's (silence 9).
- The arrowhead's size scales with the stroke width by addition (Basic Shape PRD 4.3 says only that it scales); the Line Style toggles of 4.7 are not shown while only Straight is drawn; an open head's lines are solid whatever the stroke style.
- The Property Panel has Arrow and Rectangle sections that General UI PRD Section 8 does not list, since the bar edits creation defaults only and a placed arrow's heads or rectangle's radius need a control.
- `corner_radius_mode` and the individual radii of Basic Shape PRD 5.3 and 5.4, curved and elbow arrows (4.5, 4.6), point editing, the Arc and Polygon tools, and the Freehand pipeline are the following kickoff's (decision 3).

## 7. Tests

Phase 1: `tests/test_vector_properties.py` (16: the defaults per item with the Highlighter's Flat cap, the numbered step's marker defaults and `border_style` key, the clamps, the shadow's pixels and bounding rect per item, an unfilled shape's shadow following the stroke, the two opacities lightening fill and stroke separately and reaching the arrowhead, a dashed edge's gaps, the cap beyond the endpoint, the miter corner, the Highlighter through the shared pen, a fill at zero opacity still hit, every new key's round trip per item, a c1c35c5 file loading with the defaults, unknown enum values, the mixin's stand-ins gone); `tests/test_text_item_properties.py` (5: beside `VectorItem` with the shared defaults, the callout's shadow following the tail and the cloud's circles with the bounding rect, the text box's shadow following `border_radius`, the opacities for both items, the round trip and the absent-key defaults); `tests/test_vector_bar_presets.py` (10: the order per shape tool and the Callout bar's Border Style, the percent sliders storing a fraction, bar edits reaching the next rectangle, the Text tool's Border Style reaching a new text box, a preset round trip, a stored `opacity_pct` preset applied, matched, updated, and a theme's migrated, the migration rules, the codec and the summary, a migrated session state); `tests/test_vector_panel.py` (6: a rectangle's Style, opacities, hidden single Opacity, and Shadow; a line's edits as commands with undo; a text box's hidden Appearance, Text Box rows, and Shadow with a mixed callout; a group's own Opacity beside its members' two with Shadow reaching the members; mixed values across two rectangles; tool-defaults mode hiding the fill rows for the Line tool and writing the defaults). Four existing tests moved with the work: the rectangle panel test in `tests/test_numbered_step_editing.py` expects the Shadow section, the two shape-tool bar tests and the Text tool's control list in `tests/test_tool_options_bar.py` carry the new keys, and the slider-drag test in `tests/test_property_panel_phase6.py` drags Fill Opacity.

The full suite at the step 2 commit (1f3fe67), run alone from a scratch worktree, ran 1225 tests with 13 skipped and the one environmental deselection: 1211 passed and one failed, the pre-existing timing-sensitive Zoom tool test, which passes alone. The full suite at the step 5 commit (1ec594a), the phase's last code commit, ran 1245 tests with 13 skipped and the one deselection while the Phase 2 work's targeted runs shared the machine: 1230 passed and two failed on timing, the same Zoom tool test and `tests/test_capture/test_x11.py::test_live_key_press_is_delivered`, whose log shows a `SnapScene` deleted under it by another test's teardown; both pass alone. Ruff and mypy are clean at every commit. The accessibility audit (`tests/test_accessibility.py`) passes over every changed bar and panel section.

Phase 2: `tests/test_tools/test_highlight_tool.py` (9: the PRD defaults, the four modes distinct over a coloured background with the composition mode restored, the shadow's normal composition under Multiply, the layer mode winning over the item's, the cap and dashes, the round trip with the PRD's spellings, the bar's controls with the toggles and swatches, edits and a swatch reaching the next stroke, a preset capturing the values); `tests/test_item_blend_mode.py` (4: every item type's round trip and absent-key default, the list, a rectangle in Multiply darkening the background, the panel row reaching a rectangle and a stamp with mixed and undo); `tests/test_line_spacing.py` (4: the property per paragraph with the clamp, the HTML round trip, the panel row as a command with the mixed dash and a per-paragraph undo, a new text box taking 1.2 and the editing path writing the cursor's paragraph without a command). The Phase 1 bar test's Highlighter row moved to the new list. The full suite at the Phase 2 code commit (099d4d0), run alone from a scratch worktree, ran 1262 tests with 13 skipped and the one deselection: 1248 passed and one failed, the pre-existing timing-sensitive Zoom tool test, which passes alone. Ruff and mypy are clean; the accessibility audit passes over the Highlighter's bar and the new panel rows.

Phase 3: `tests/test_arrow_heads.py` (7: the defaults and the named, custom, and stroke-scaled sizes; each style's pixels at the head with the shaft ending at a filled base; the tail's own style with the hit shape; the stroke opacity on a head and the shadow under the tail; the keys' round trip with an old file's Filled head and the codec; the bar's dropdowns with glyphs and the custom spin box reaching the next arrow; the panel's Arrow section with mixed and undo); `tests/test_corner_radius.py` (3: stored as set and drawn clamped with a resize restoring it, the bar control reaching a new rectangle, the panel row with mixed and undo). The two bar tests carry the new keys. The full suite at the Phase 3 code commit (ab71e15), run alone from a scratch worktree, ran 1272 tests with 13 skipped and the one deselection: 1258 passed and one failed, the pre-existing timing-sensitive Zoom tool test, which passes alone. Ruff and mypy are clean; the accessibility audit passes over the Arrow bar's new dropdowns and the two new panel sections.

## 8. Close-out of the work

Every phase is done. The General UI notes' Section 5.1 decision 6.2 row and Section 6's bullets on the shared controls and the per-tool bar contents are closed, with a Section 21 pointer (General UI notes 1.33); the marker notes' Section 6 deviations on the three-item shadow and the numbered step's three opacity controls are closed (1.5); the Snagit notes carry the unmapped properties and the reader's Filled head (1.3). The following kickoff is written: `docs/Basic-Shape-Remainder-Kickoff-Prompt.md` (revision 1.0), the Basic Shape PRD's remainder (point editing, curved and elbow arrows, the individual corner radii, the Freehand pipeline, the Arc and Polygon tools) and the Blur tool's modes, five phases with four decisions and six silences.

Display checks, run by Doug on 09-12-26 from a checklist page whose marks and notes were read back. **Answered and passing**, three of the four this work owed: a rectangle with a shadow and a dashed stroke at 50 percent fill opacity ("perfect"), an arrow with a diamond head and an open tail ("perfect"), and a text box at 2.0 line spacing ("perfect"). The three marker tools owed from before this work also pass, and are recorded in the marker notes' Section 7.

**Still owed:** a highlight in Multiply over dark text, which was marked without a note and is unrecorded; and the Zoom tool display check of the General UI notes' Section 19.2 (row 20), whose Alt+click half was answered on 09-12-26 — "Alt still does not work" — and whose right-click half is still unanswered. Verified on this machine 09-12-26: `org.cinnamon.desktop.wm.preferences mouse-button-modifier` reads `<Alt>`, which is the cause row 20 already suspected.

The following kickoff ran on 09-11-26 and is complete: `docs/Basic-Shape-Remainder-Implementation.md` (revision 1.3) holds its record, including the display checks it adds.

**Next required step (as written at this close-out):** the work is complete. Two candidates are known: the Basic Shape remainder and Blur modes kickoff (`docs/Basic-Shape-Remainder-Kickoff-Prompt.md`, revision 1.0, starting state at commit ab71e15), which runs on this machine and is recommended next; and the Windows backend kickoff (`docs/Windows-Backend-Kickoff-Prompt.md`), which waits for a Windows machine. Before either, the display checks above.

## Change Log

| Rev | Date (MM-DD-YY HH:MM) | Author | Change |
|---|---|---|---|
| 1.5 | 09-12-26 09:58 | Claude (Claude Code) | Section 8: the display checks Doug ran on 09-12-26, quoted. The shadowed dashed rectangle, the diamond-and-open arrow, and the 2.0 line spacing pass, as do the three marker checks owed from before this work. The Multiply highlight and the Zoom tool's row 20 stay owed. |
| 1.4 | 09-11-26 18:44 | Claude (Claude Code) | Section 8 points to the completed Basic Shape remainder work. |
| 1.3 | 09-11-26 14:21 | Claude (Claude Code) | Phase 3 done and the work complete: the phase table, Section 5 (what Phase 3 built in one commit, the silences found while building), Section 6 (three deviations added), Section 7 (the tests and the suite run), Section 8 (the close-out of the work, the display checks owed, the next required step). Basic Shape PRD 1.8, General UI PRD 2.16, Technical Architecture PRD 1.28; General UI notes 1.33, marker notes 1.5, Snagit notes 1.3; `docs/Basic-Shape-Remainder-Kickoff-Prompt.md` 1.0. |
| 1.2 | 09-11-26 13:33 | Claude (Claude Code) | Phase 2 done: the phase table, Section 4 (what Phase 2 built in one commit, the silences found while building), Section 5 (three deviations added, the SVG one widened), Section 6 (the tests and the suite run), the next required step. Blur PRD 1.4, Text and Callout PRD 1.9, General UI PRD 2.15, Technical Architecture PRD 1.27, Snagit notes 1.2. |
| 1.1 | 09-11-26 12:52 | Claude (Claude Code) | Phase 1 done: the phase table, Section 3 (what Phase 1 built, the silences found while building, the two bugs fixed), Section 4 (four deviations added), Section 5 (the tests and the suite runs), the next required step. Basic Shape PRD 1.7, Text and Callout PRD 1.8, General UI PRD 2.14, Technical Architecture PRD 1.26. |
| 1.0 | 09-11-26 11:13 | Claude (Claude Code) | Initial notes: the starting state, the phase table, the four decisions (1 B, 2 A, 3 B, 4 A) and the twelve silences as chosen 09-11-26, three findings from the read list, the deviations they imply, the next required step. Basic Shape PRD 1.6, Text and Callout PRD 1.7, General UI PRD 2.13, Technical Architecture PRD 1.25. |
